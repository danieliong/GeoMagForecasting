# @package _global_

defaults:
  - hydra/sweeper: nevergrad
  # - hydra/launcher: submitit_slurm
  - model: xgboost

hydra:
  sweep:
    dir: multirun/${model}/${now:%Y-%m-%d_%H-%M}
    subdir: ${hydra.job.override_dirname}
  job:
    config:
      override_dirname:
        kv_sep: '-'
        item_sep: "_"
  # job_logging:
  #   level: INFO
  #   handlers:
  #   - file

lag: "H"
exog_lag: "H"
lead: "H"

features: solar_wind
target: supermag
overrides: ''

# DON'T OVERRIDE/CHANGE
inputs:
  dir: outputs/process_data/features=${features},target=${target}/${overrides}
  features_pipeline: "features_pipeline.pkl"
  target_pipeline: "target_pipeline.pkl"
  train_features: "train_features.pkl"
  train_target: "train_target.pkl"
  test_features: "test_features.pkl"
  test_target: "test_target.pkl"


split:
  # Use sklearn's TimeSeriesSplit
  method: "timeseries"
  n_splits: 5
  # test_size: .2 # Needs to be int here

metric: "rmse"

# Inverse transform before computing metrics
inverse_transform: true
